language: php
php:
  - '5.5'
  - '7.0'
  - hhvm

sudo: required

before_install:
  - sudo apt-get update -q
  - sudo apt-get install calibre-bin default-jre -y

install:
  - make install

script:
  - make test
  - make composer-test
  - make integration-test
